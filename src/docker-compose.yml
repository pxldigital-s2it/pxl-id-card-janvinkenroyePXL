version: '3.4'

services:
  presences.api:
    image: ${DOCKER_REGISTRY-}presencesapi
    build:
      context: .
      dockerfile: Services/Presences/Presences.Api/Dockerfile
    depends_on:
        - sqldata

  sqldata:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: sqldata

